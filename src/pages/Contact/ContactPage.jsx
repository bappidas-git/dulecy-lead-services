/* ============================================
   ContactPage — stub
   --------------------------------------------
   Placeholder carrying only the mockup's hero headline
   (`mockup/contact.html`). The contact cards and the embedded lead
   form land in Prompts 06–07.
   ============================================ */

import React from 'react';
import styles from './ContactPage.module.css';

const ContactPage = () => (
  <section className={styles.hero}>
    <p className="eyebrow">Contact</p>
    <h1 className="display">
      Let&rsquo;s build <em className="serif grad-text">what comes next.</em>
    </h1>
  </section>
);

export default ContactPage;
